import { describe, it, expect } from 'vitest'
// import { retrieveRelevantChunks } from './retrieval'  // skipped — requires Firebase

describe('retrieval (smoke test)', () => {
  it.skip('busca chunks relevantes (requer Firebase)', () => {
    // Teste real requer emulador rodando
    expect(true).toBe(true)
  })
})
